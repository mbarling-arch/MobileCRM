import React from 'react';
import { Typography } from '@mui/material';

const SoldList = ({ assets }) => {
  return (
    <Typography sx={{ color: 'text.secondary', textAlign: 'center', py: 4 }}>
      Sold assets view - coming soon
    </Typography>
  );
};

export default SoldList;

